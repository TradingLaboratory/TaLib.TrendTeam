// Этот файл является частью библиотеки технического анализа для .NET.
// Название файла: FunctionHelpers.cs

namespace TALib;
/// <summary>
/// В этом классе собраны вспомогательные методы для расчёта индикаторов
/// </summary>
internal static partial class FunctionHelpers
{
    // Раньше были в таком порядке:

    // 1. CalcExponentialMA.cs — Расчет экспоненциальной скользящей средней (EMA)
    // 2. CalcMACD.cs — Расчет MACD с сигнальной линией и гистограммой
    // 3. CalcPriceOscillator.cs — Расчет ценового осциллятора на основе двух MA
    // 4. CalcSimpleMA.cs — Расчет простой скользящей средней (SMA)
    // 5. CalcVariance.cs — Расчет дисперсии временного ряда
    // 6. CalcAccumulationDistribution.cs — Расчет индикатора накопления/распределения (AD)
    // 7. CalcLowest.cs — Поиск минимального значения в скользящем окне
    // 8. CalcHighest.cs — Поиск максимального значения в скользящем окне
    // 9. InitDMAndTR.cs — Инициализация Directional Movement и True Range
    // 10. UpdateDMAndTR.cs — Обновление Directional Movement и True Range
    // 11. CalcDeltas.cs — Расчет дельт направленного движения (+DM/-DM)
    // 12. CalcDI.cs — Расчет индексов направленного движения (+DI/-DI)
    // 13. TrueRange.cs — Расчет истинного диапазона (True Range)
    // 14. DoPriceWma.cs — Расчет взвешенного скользящего среднего (WMA)

}

